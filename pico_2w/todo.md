# TODO

- Add light sensor
- add matter support
- Fixup display crate to be async
    currently is causing quite the delay when it runs and upates the display
- fixup tui so that it reconnects properly
    seems like currently when it looses a device it never reconnects
- Explore removing display-interface from ssd1860 while still keeping async
    pretty low prio since it currently works fine



